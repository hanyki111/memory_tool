# 시간-공간 통합 지식 체계 v2.0

## 목차

1. 핵심 개념
2. 철학과 원리
3. 시스템 구조
4. 메타데이터와 스키마
5. 생명주기와 흐름
6. 실전 운영 가이드
7. 도구와 명령어
8. 실전 예시
9. 안전과 확장
10. 시작하기

---

## 핵심 개념

### 한 문장 정의

**시간축(Timeline)에서 모든 것을 0.5초로 포착하고, 공간축(Modules/Concepts)으로 주기적 정리하며, 프로젝트별 격리와 개인 KB 공통 참조로 확장하는 완전한 지식 관리 체계**

### 왜 "완전한" 체계인가?

기존 지식 관리 시스템(Notion, Obsidian, Wiki, PARA)은 현재 상태만을 기록합니다. 마치 영화의 마지막 장면만 보여주는 것과 같습니다. 우리는 결과만 보고, 어떻게 그 결론에 도달했는지는 알 수 없습니다.

이 시스템은 **시간(진화)**과 **공간(구조)**이라는 두 개의 직교하는 축을 통해 지식의 완전한 기록을 만듭니다.

```
        공간 축 (구조, 관계, 현재)
             ↑
             │
             │  각 지식은
             │  시간×공간 좌표를 가짐
             │
    ─────────┼─────────→ 시간 축 (진화, 맥락, 이유)
             │
         (현재)
```

### 완전성의 세 가지 차원

**1차원: 내용 (What)**
- 개념의 정의
- 모듈의 구조
- 시스템의 상태

**2차원: 관계 (How)**
- 개념 간 연결
- 모듈 간 의존성
- 적용 사례

**3차원: 시간 (When/Why)**
- 이해의 진화
- 결정의 배경
- 맥락의 보존

---

## 철학과 원리

### 핵심 철학

#### 1. 두 가지 관점으로 본 같은 지식

모든 지식은 두 가지 방식으로 이해될 수 있습니다:

**시간적 관점 (How it became):**

```markdown
2025-11-01 | "시장 효율성" 개념 처음 접함
2025-11-05 | EMH 논문 읽음 - 세 가지 형태 학습
2025-11-10 | 실제 시장은 완전히 효율적이지 않다는 것 깨달음
2025-11-15 | 트레이딩 시스템 설계에 적용
```

이것은 **줄거리**입니다. 이해가 어떻게 진화했는지, 왜 지금의 결론에 도달했는지를 보여줍니다.

**공간적 관점 (What is):**

```markdown
# Market Efficiency

## 정의
시장 가격이 이용 가능한 모든 정보를 반영하는 정도

## 형태
- 약형: 과거 가격 정보 반영
- 준강형: 공개 정보 반영
- 강형: 모든 정보 반영

## 현실
실제 시장은 완전히 효율적이지 않음
→ 비효율성을 찾는 것이 기회

## 적용
[[modules/trading-system]]에서 활용
```

이것은 **현재 이해**입니다. 지금 우리가 아는 것을 구조화하고 정리한 형태입니다.

**둘 다 필요한 이유:**
- 시간만: 결과가 보이지 않음 (일기장만 있는 상태)
- 공간만: 맥락이 사라짐 (위키만 있는 상태)
- 시간+공간: 완전한 지식 (과정과 결과)

#### 2. 왜 Git과 비슷한가?

Git을 아신다면 이 시스템을 쉽게 이해할 수 있습니다:

| Git | 이 시스템 |
|-----|----------|
| Commits (시간) | Timeline |
| Files/Directories (공간) | Modules/Concepts |
| `git log` | Timeline 탐색 |
| `git blame` | 결정의 맥락 추적 |
| Branches | 프로젝트 격리 |

차이점: Git은 코드의 시공간, 이 시스템은 **지식의 시공간**

### 5대 설계 원칙

#### 원칙 1: Time First (시간 우선)

**"먼저 포착, 나중 정리"**

❌ 잘못된 방식:
```
"이건 어느 폴더에 넣지?" (고민 5분)
"나중에 정리하자" (그리고 잊어버림)
```

✅ 올바른 방식:
```bash
m "OAuth 구현 중 발견한 것"  # 3초, 즉시
# → 주말에 정리
```

이유:
- 생각하는 순간 포착하지 않으면 사라짐
- 분류는 나중에 패턴이 보일 때 하는 것이 정확
- 완벽한 조직보다 완전한 기록이 우선

#### 원칙 2: Lossless (무손실)

**"모든 것을 기록, 아무것도 잃지 않음"**

Timeline은 완전한 역사입니다:
- 중요한 것도, 사소한 것도
- 성공도, 실패도
- 확신도, 의심도

예시:

```markdown
# timeline/2025-11/12.md
- 09:00 | OAuth 구현 시작
- 10:00 | JWT vs 세션 고민
- 11:00 | JWT가 나을 것 같음
- 14:00 | 아니다, 세션이 더 안전
- 15:00 | 결정: 세션 방식 선택
```

이 "고민의 흔적"이 중요합니다. 3개월 후 "왜 JWT 안 썼지?"라고 물을 때, Timeline을 보면 당시 고민을 재현할 수 있습니다.

#### 원칙 3: Minimal Friction (최소 마찰)

**"입력은 최소, 정리는 나중"**

입력 시:
```bash
m "생각나는 대로 기록"
# 제목 없음, 태그 없음, 분류 없음
```

정리 시 (주말):
```yaml
---
id: auto-generated
type: concept  # 이제 분류
title: "Market Efficiency"  # 이제 제목
tags: [economics, trading]  # 이제 태그
---
```

**0.5초 캡처의 비밀:**
- 타이핑만 하면 됨
- 생각하지 않음
- 결정하지 않음
- 그냥 기록

#### 원칙 4: Loose Coupling (느슨한 결합)

**"프로젝트는 격리, 지식은 공유"**

```
projectA/.memory/     (독립)
  └─ 참조 → ~/memory/personal/  (공통)

projectB/.memory/     (독립)
  └─ 참조 → ~/memory/personal/  (공통)
```

이유:
- 프로젝트는 삭제/아카이브 가능
- 하지만 배운 지식은 보존
- 다음 프로젝트에서 재활용

#### 원칙 5: Local First (로컬 우선)

**"기본은 로컬, 확장은 명시적"**

검색:
```bash
ms "keyword"           # 현재 프로젝트만 (기본)
ms --with-kb "keyword" # + 개인 KB (명시적)
ms --all "keyword"     # 모든 프로젝트 (희귀)
```

**이유:**
- 오염 방지 (무관한 정보 혼입 X)
- 속도 (검색 범위 제한)
- 명확성 (어디서 온 정보인지 명확)

---

## 시스템 구조

### 3층 아키텍처

이 시스템은 세 개의 독립적이면서도 연결된 레이어로 구성됩니다.

#### Layer 1: 프로젝트 Memory (독립 네임스페이스)

각 프로젝트는 완전히 독립적인 `.memory/` 디렉토리를 가집니다.

```
~/projects/trading-system/.memory/
  timeline/           # 시간축: 모든 작업의 연대기
    2025-11/
      01.md
      02.md
      ...
      12.md         # 오늘
  
  modules/            # 공간축: 모듈별 컨텍스트
    order-processing/
      module.md       # 모듈 정의
      current.md      # 현재 상태
      decisions.md    # 결정 사항
      dependencies.md # 의존성
      interface.md    # 인터페이스
    
    market-analysis/
      ...
  
  concepts/           # 프로젝트 특화 개념
    order-state-machine.md
    price-calculation.md
  
  .index/             # 자동 생성 (사용자 직접 수정 X)
    embeddings.db     # 의미 검색용
    timeline.idx      # 시간 인덱스
    graph.db          # 관계 그래프
  
  kb.lock             # KB 참조 설정
```

**핵심 특징:**
- Git처럼 프로젝트마다 `.memory/` 존재
- 프로젝트 삭제 = memory도 삭제 (선택적 아카이브)
- 완전한 격리로 프로젝트 간 오염 없음

#### Layer 2: 개인 Knowledge Base (공통 지식 레이어)

범용 지식의 단일 원본(Single Source of Truth)입니다.

```
~/memory/personal/
  concepts/           # 범용 개념 (여러 프로젝트 참조 가능)
    economics/
      market-efficiency.md
      supply-demand.md
      elasticity.md
    
    design-patterns/
      state-pattern.md
      strategy-pattern.md
      observer-pattern.md
    
    algorithms/
      binary-search.md
      dynamic-programming.md
  
  resources/          # 참고 자료, 논문 요약
    papers/
      fama-1970-efficient-markets.md
    
    books/
      design-patterns-gof.md
  
  .index/
    embeddings.db
```

핵심 특징:
- 프로젝트와 독립적
- 한 번 정리하면 모든 프로젝트에서 참조
- 장기 보존 (프로젝트 삭제와 무관)

#### Layer 3: 통합 검색 (스코프 규범)

세 가지 검색 스코프:

```bash
# 1. 로컬 (기본)
cd ~/projects/trading-system
ms "OAuth"
→ trading-system/.memory/ 내부만

# 2. KB 포함 (명시적)
ms --with-kb "market efficiency"
→ trading-system/.memory/ + ~/memory/personal/

# 3. 전체 (희귀)
ms --all "latency budget"
→ 모든 projects/*/.memory/ + ~/memory/personal/
```

**스코프 선택 가이드:**

| 상황 | 스코프 | 예시 |
|------|--------|------|
| 이 프로젝트에서 뭐 했더라? | 로컬 | `ms "OAuth"` |
| 이 개념 배운 적 있나? | KB 포함 | `ms --with-kb "state pattern"` |
| 어느 프로젝트에서 썼더라? | 전체 | `ms --all "Redis"` |

### 디렉토리 구조 상세

#### Timeline 구조

```
.memory/timeline/
  2025-01/          # 월별 폴더
    01.md           # 일별 파일
    02.md
    ...
  2025-02/
    01.md
    ...
  2025-11/          # 현재 월
    12.md           # 오늘
```

파일 내용 예시:

```markdown
# timeline/2025-11/12.md

- 09:00 | 프로젝트 시작, OAuth 통합 계획
- 09:30 | Passport.js vs NextAuth 조사 시작
- 11:00 | Passport가 더 유연해 보임, 전략 패턴
- 11:30 | 점심 전 결정: Passport.js 선택
- 14:00 | Google OAuth 전략 구현 시작
- 14:30 | 환경 변수 설정, callback URL 고민
- 15:00 | callback URL은 /api/auth/google/callback로 결정
- 16:00 | 기본 구현 완료, 테스트 필요
- 16:30 | 로컬 테스트 성공
- 17:00 | 내일: GitHub OAuth 추가, 에러 처리
```

**특징:**
- 시간순 정렬
- 자유로운 형식
- 중간 생각도 기록 ("~해 보임", "고민")
- 결정 순간 명확히 표시 ("결정:")

#### Modules 구조

각 모듈은 5개의 표준 파일을 가집니다:

```
modules/<module-name>/
  module.md         # 정의: 이 모듈은 무엇인가?
  current.md        # 상태: 지금 무엇을 하고 있나?
  decisions.md      # 결정: 왜 이렇게 했나?
  interface.md      # 사용: 어떻게 쓰나?
  dependencies.md   # 의존: 무엇이 필요한가?
```

각 파일의 역할:

**1. module.md - 모듈 정의**

```markdown
---
type: module
created: 2025-11-01
status: active
---

# User Authentication Module

## Purpose
사용자 인증 및 세션 관리를 담당

## Scope
- 로그인/로그아웃
- 세션 관리
- OAuth 2.0 통합 (Google, GitHub)
- 비밀번호 재설정

## Out of Scope
- 권한 관리 (authorization 모듈)
- 사용자 프로필 관리 (user-profile 모듈)
- 이메일 전송 (notification 모듈)

## Architecture
- Strategy Pattern (Passport.js)
- Session-based authentication
- Redis session store

## Related Modules
- [[modules/user-profile]] (사용자 정보)
- [[modules/authorization]] (권한 체크)
```

**2. current.md - 현재 상태**

```markdown
# Current Status

## 2025-11-15 금요일
### Completed
- [x] Google OAuth 통합 완료
- [x] 세션 미들웨어 설정
- [x] 기본 로그인/로그아웃 API

### In Progress
- [ ] GitHub OAuth 통합 (80% 완료)
  - 전략 설정 완료
  - 프론트엔드 버튼 추가 필요
- [ ] 세션 토큰 갱신 로직 (30%)

### Blocked
- [ ] 비밀번호 재설정
  - 대기: notification 모듈 완성

## Next Week
1. GitHub OAuth 마무리
2. 에러 처리 강화
3. 테스트 케이스 추가 (목표: 90% 커버리지)

## Known Issues
- 세션 만료 시 리다이렉트 깜빡임 (UX 개선 필요)
- 동시 로그인 처리 미흡
```

**3. decisions.md - 결정 사항**

```markdown
# Key Decisions

## 2025-11-02: 인증 방식 선택
**결정:** 세션 기반 인증
**대안 검토:**
- JWT: 무상태, 확장성 우수
- OAuth only: 외부 의존성

**선택 이유:**
1. 서버 제어 강화 (즉시 무효화 가능)
2. 보안 정책 일관성 (CSRF 방어)
3. 초기 단계에서 충분한 확장성

**트레이드오프:**
- 단점: Redis 의존성, 서버 메모리 사용
- 수용: 현재 규모에서 문제없음, 나중에 재평가

**컨텍스트:** [[time:2025-11/02#15:00]]
**참고 개념:** [[kb:session-vs-jwt]]

---

## 2025-11-12: OAuth 라이브러리 선택
**결정:** Passport.js
**대안 검토:**
- NextAuth: Next.js 특화, 간편
- Auth0: SaaS, 비용 발생

**선택 이유:**
1. 전략 패턴으로 확장성 우수
2. 기존 Express + 세션과 자연스러운 통합
3. 커뮤니티 성숙, 다양한 전략 지원

**컨텍스트:** [[time:2025-11/12#11:00]]
**관련 개념:** [[kb:strategy-pattern]]

---

## 2025-11-15: GitHub OAuth Scope
**결정:** `user:email`, `read:user`
**이유:**
- 최소 권한 원칙
- 현재는 이메일과 기본 프로필만 필요
- 나중에 필요하면 확장 가능

**컨텍스트:** [[time:2025-11/15#14:00]]
```

결정 문서 작성 규칙:
- 모든 중요 결정 기록
- 대안도 함께 기록 (왜 선택하지 않았는지)
- 트레이드오프 명시
- 반드시 Timeline 컨텍스트 링크

**4. interface.md - 인터페이스**

```markdown
# Interface

## API Endpoints

### POST /api/auth/login
**Request:**

{
  "email": "user@example.com",
  "password": "secret"
}


**Response (Success):**

{
  "user": {
    "id": "user_123",
    "email": "user@example.com",
    "name": "John Doe"
  }
}


**Response (Error):**

{
  "error": "INVALID_CREDENTIALS"
}


### GET /api/auth/oauth/google
OAuth 인증 시작 (리다이렉트)

### GET /api/auth/oauth/google/callback
OAuth 콜백 핸들러

### POST /api/auth/logout
현재 세션 무효화

## Usage Example
// 로그인
const response = await fetch('/api/auth/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ email, password })
});

// 세션 확인
const user = await fetch('/api/auth/me');


## Session Structure

{
  userId: 'user_123',
  email: 'user@example.com',
  provider: 'local' | 'google' | 'github',
  createdAt: '2025-11-15T09:00:00Z',
  expiresAt: '2025-11-15T21:00:00Z'
}

```

**5. dependencies.md - 의존성**

```markdown
# Dependencies

## Code Dependencies

### Direct
- `express-session`: 세션 관리
- `passport`: OAuth 전략
- `passport-google-oauth20`: Google 전략
- `passport-github2`: GitHub 전략
- `bcrypt`: 비밀번호 해싱

### Indirect
- `redis`: 세션 저장소
- `connect-redis`: Redis 세션 어댑터

## Module Dependencies

### Depends On
- [[modules/database]] - 사용자 조회/저장
- [[modules/config]] - 환경 변수 관리

### Used By
- [[modules/api]] - API 라우트에서 인증 체크
- [[modules/admin]] - 관리자 페이지 보호

## External Services
- Google OAuth API
- GitHub OAuth API

## Environment Variables
```bash
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
GITHUB_CLIENT_ID=...
GITHUB_CLIENT_SECRET=...
SESSION_SECRET=...
REDIS_URL=...
```


#### Concepts 구조

개념은 더 자유로운 형식입니다:

```
concepts/
  <domain>/
    <concept-name>.md
```

개념 문서 예시:

```markdown
---
id: market-efficiency-20251101
type: concept
domain: economics
tags: [finance, trading, theory]
created: 2025-11-01
updated: 2025-11-15

# Timeline 참조
timeline_refs:
  - [[time:2025-11/01#14:00]]  # 첫 학습
  - [[time:2025-11/05#10:00]]  # EMH 학습
  - [[time:2025-11/15#11:00]]  # 프로젝트 적용

# 관계
prerequisites: [[basic-economics]], [[probability]]
related: [[efficient-market-hypothesis]], [[behavioral-economics]]
applied_in: [[modules/trading-system/market-analysis]]
---

# Market Efficiency

## 정의

시장 효율성(Market Efficiency)은 시장 가격이 이용 가능한 모든 정보를 얼마나 빠르고 정확하게 반영하는지를 나타내는 개념입니다.

## 진화 과정

### 2025-11-01: 첫 접촉
처음에는 단순히 "가격 = 정보"로 이해했습니다. 시장이 효율적이면 가격이 공정하다는 의미인 줄 알았습니다.

### 2025-11-05: 이론 학습
효율적 시장 가설(EMH)을 배우면서 세 가지 형태가 있다는 것을 알게 되었습니다:

**약형 효율성 (Weak Form)**
- 과거 가격 정보가 현재 가격에 반영됨
- 기술적 분석으로 초과 수익 불가능

**준강형 효율성 (Semi-Strong Form)**
- 모든 공개 정보가 반영됨
- 공개 정보 기반 분석으로 초과 수익 불가능

**강형 효율성 (Strong Form)**
- 내부 정보까지 모두 반영됨
- 어떤 정보로도 초과 수익 불가능

### 2025-11-10: 현실 인식
논문과 사례를 읽으면서 실제 시장은 완전히 효율적이지 않다는 것을 깨달았습니다:
- 행동경제학적 이상 현상(anomalies)
- 정보 비대칭
- 거래 비용과 마찰
- 투자자 편향

### 2025-11-15: 실전 적용
트레이딩 시스템 설계에 반영:
- 완전 효율성 가정 ❌
- 대신 "대체로 효율적이지만 비효율성 존재" 가정
- 비효율성을 찾는 것이 전략의 핵심

## 핵심 통찰

**시장은 대체로 효율적이지만, 그 비효율성을 찾는 것이 기회다.**

완전 효율 가정하에서는 모든 거래 전략이 무의미하지만, 현실의 마찰과 비효율성이 기회를 만듭니다.

## 실전 적용

[[modules/trading-system]]에서:
1. 시장 효율성 전제로 시작
2. 비효율성 패턴 탐지
3. 통계적 유의성 검증
4. 전략 구현

## 추가 학습 자료

- [[kb:efficient-market-hypothesis]] - 이론 상세
- [[kb:behavioral-economics]] - 비효율성의 원인
- [[resources/papers/fama-1970]] - 원본 논문
```

---

## 메타데이터와 스키마

### 코어 스키마 (모든 문서 공통)

모든 마크다운 문서는 YAML frontmatter로 시작합니다.

```yaml
---
# 필수 필드 (7개)
id: 20251112T221523.104-8421-f3   # 고유 ID
type: log | module | concept | decision
title: ""                          # 비어있어도 됨
privacy: internal                  # public | internal | secret
tags: []
links: []
created: 2025-11-12T22:15:23Z
updated: 2025-11-12T22:15:23Z

# 선택 필드 (타입별로 다름)
# ...
---
```

### 필드 상세 설명

**id (필수)**

형식: `YYYYMMDDTHHmmss.mmm-PID-RAND`
예시: `20251112T221523.104-8421-f3a2`

특징:
- 단조 증가 (Monotonic): 시간순 정렬 가능
- 충돌 방지: 타임스탬프 + PID + 랜덤
- 파일명과 무관: 파일 이동/리네임해도 ID 유지

생성 방법:
```bash
# 자동 생성 (도구가 처리)
id=$(date -u +%Y%m%dT%H%M%S.%3N)-$$-$(openssl rand -hex 2)
```

**type (필수)**

값:
- `log`: Timeline 항목
- `module`: 모듈 문서
- `concept`: 개념 설명
- `decision`: 결정 사항 (단독 문서인 경우)

사용:
- 검색 필터: `ms --type concept "pattern"`
- 자동 분류 기준
- LLM 컨텍스트 구성 시 우선순위

**title (선택)**

입력 시점:
- Timeline: 비워둠 (첫 줄이 제목 역할)
- Modules/Concepts: 나중에 채움

자동 제안:
```bash
# 도구가 첫 줄 또는 내용 분석해서 제안
title: "OAuth Implementation Discussion"
```

**privacy (필수)**

값:
- `public`: 외부 공유 가능 (블로그 포스트 등)
- `internal`: 개인/팀 내부 (기본값)
- `secret`: 민감 정보 (API 키, 비밀번호 등)

강제 규칙:
```python
if privacy == "secret":
    exclude_from_llm_context()
    exclude_from_search(scope="--with-kb")
    exclude_from_cross_links()
```

예시:
```yaml
---
privacy: secret
---
# API Keys

OPENAI_API_KEY=sk-...   # 절대 LLM에 전달되지 않음
```

**tags (선택)**

자유로운 태그:
```yaml
tags: [economics, trading, algorithm, optimization]
```

활용:
```bash
ms --tag economics "efficiency"
```

**links (필수)**

ID 기반 링크만 허용:
```yaml
links:
  - [[kb:market-efficiency]]      # 개인 KB
  - [[doc:20251112T...]]          # 같은 프로젝트
  - [[time:2025-11/12#14:00]]     # Timeline
```

절대 경로 금지:
```yaml
# ❌ 잘못된 예
links:
  - ../../../personal/concepts/economics/market.md
  - /Users/john/memory/personal/concepts/...

# ✅ 올바른 예
links:
  - [[kb:market-efficiency]]
```

### 타입별 추가 필드

**Module 타입**

```yaml
---
type: module
# ... 기본 필드 ...

# Module 전용
status: active | deprecated | archived
dependencies:
  - modules/database
  - modules/config
architecture: microservice | monolith | library
---
```

**Concept 타입**

```yaml
---
type: concept
# ... 기본 필드 ...

# Concept 전용
domain: economics | design-patterns | algorithms
prerequisites:
  - [[kb:basic-economics]]
  - [[kb:probability]]
timeline_refs:
  - [[time:2025-11/01#14:00]]
  - [[time:2025-11/05#10:00]]
difficulty: beginner | intermediate | advanced
---
```

### 스키마 진화 원칙

**불변 필드:**
- `id`: 절대 변경 불가
- `type`: 거의 변경 안 함
- `created`: 절대 변경 불가

**가변 필드:**
- `title`: 언제든지 개선
- `tags`: 추가/제거 자유
- `links`: 계속 확장
- `updated`: 자동 업데이트

**확장:**
- 새 필드 추가는 자유
- 기존 필드 제거는 신중히

---

## 생명주기와 흐름

### 지식의 4단계 생명주기

```
[1. 포착] → [2. 탐색] → [3. 정리] → [4. 활용]
    ↓          ↓           ↓          ↓
Timeline   Search      Promote    LLM Context
```

#### 단계 1: 포착 (Capture)

**도구:** `m "..."`
**시점:** 실시간, 즉시

예시:
```bash
# 09:00 - 작업 시작
m "OAuth 통합 작업 시작"

# 09:30 - 조사 중
m "Passport.js vs NextAuth 조사"

# 10:00 - 생각
m "Passport가 더 유연해 보임, 전략 패턴"

# 11:00 - 결정
m "결정: Passport.js 선택"

# 11:30 - 이유
m "이유: 기존 세션과 통합 쉬움, 확장성"

# 14:00 - 진행
m "Google OAuth 전략 구현 중"

# 16:00 - 완료
m "Google OAuth 기본 구현 완료"

# 17:00 - 내일
m "내일: GitHub OAuth, 에러 처리"
```

결과: `timeline/2025-11/12.md`

```markdown
- 09:00 | OAuth 통합 작업 시작
- 09:30 | Passport.js vs NextAuth 조사
- 10:00 | Passport가 더 유연해 보임, 전략 패턴
- 11:00 | 결정: Passport.js 선택
- 11:30 | 이유: 기존 세션과 통합 쉬움, 확장성
- 14:00 | Google OAuth 전략 구현 중
- 16:00 | Google OAuth 기본 구현 완료
- 17:00 | 내일: GitHub OAuth, 에러 처리
```

핵심:
- 생각하지 않고 기록
- 분류하지 않고 기록
- 정제하지 않고 기록
- 그냥 기록

#### 단계 2: 탐색 (Search)

**도구:** `ms "..."`
**시점:** 필요할 때

**시나리오 1: 오늘 뭐 했더라?**
```bash
ms "OAuth"
# → timeline/2025-11/12.md 발견
# → 오늘 OAuth 관련 작업 확인
```

**시나리오 2: 이 개념 배웠나?**
```bash
ms --with-kb "strategy pattern"
# → kb:strategy-pattern 발견
# → 과거 학습 내용 확인
```

**시나리오 3: 어디서 썼더라?**
```bash
ms --all "Passport.js"
# → projectA/.memory/modules/auth/
# → projectB/.memory/timeline/2025-09/
# → 여러 프로젝트에서 사용 흔적 발견
```

#### 단계 3: 정리 (Organize)

**시점:** 주말 또는 여유 있을 때

**과정:**

**Step 1: Timeline 리뷰**

```bash
# 이번 주 Timeline 확인
cat .memory/timeline/2025-11/{08..12}.md

# 또는
ms --since 7days "결정:"
```

**Step 2: 패턴 인식**

Timeline을 읽으면서 패턴을 찾습니다:

```
패턴 발견:
- "Passport.js 선택" → 중요한 결정
- "전략 패턴" → 개념 학습
- "Google OAuth 완료" → 진행 상황
```

**Step 3: 승격**

결정 승격:
```bash
cat >> .memory/modules/auth/decisions.md << 'EOF'

## 2025-11-12: OAuth 라이브러리 선택
**결정:** Passport.js
**대안:** NextAuth, Auth0
**이유:**
- 전략 패턴으로 확장성
- 기존 세션과 통합 자연스러움
**컨텍스트:** [[time:2025-11/12#11:00]]
**관련 개념:** [[kb:strategy-pattern]]
EOF
```

진행 상황 업데이트:
```bash
cat >> .memory/modules/auth/current.md << 'EOF'

## 2025-11-12
- [x] OAuth 라이브러리 선택: Passport.js
- [x] Google OAuth 기본 구현
- [ ] GitHub OAuth (다음주)
EOF
```

개념 추가 (필요시):

만약 "전략 패턴"을 개인 KB에 추가하려면:

```bash
cat > ~/memory/personal/concepts/design-patterns/strategy-pattern.md << 'EOF'
---
id: strategy-pattern
type: concept
domain: design-patterns
---

# Strategy Pattern

## 정의
알고리즘군을 정의하고 캡슐화하여 
각각을 교환 가능하게 만드는 패턴

## 실제 적용
- [[time:2025-11/12#10:00]] - Passport.js에서 발견
- `projectA/.memory/modules/auth` - OAuth 구현에 활용
EOF
```

#### 단계 4: 활용 (Use)

**시점:** 다음 작업, LLM과 대화

**시나리오: Claude Code와 작업**

```bash
claude "OAuth에 GitHub 추가하는 방법"
```

백그라운드에서 일어나는 일:

1. 컨텍스트 자동 구성:
```
.claude/build-context.sh 실행
  ↓
Timeline 최근 7일 로드
  - 2025-11/12.md 포함 (OAuth 작업)
  ↓
Modules 로드
  - auth/module.md (정의)
  - auth/current.md (현재 상태)
  - auth/decisions.md (Passport 선택 이유)
  ↓
.claude/memory-context.md 생성
```

2. Claude에게 전달:
```
Query: "OAuth에 GitHub 추가하는 방법"
Context: [memory-context.md]
```

3. Claude의 답변:
```
현재 Passport.js를 사용 중이시군요. 
(decisions.md에서 확인)

Google OAuth가 이미 구현되어 있으므로
(timeline에서 확인)

같은 패턴으로 GitHub를 추가하면 됩니다:

1. passport-github2 설치
2. GitHubStrategy 추가
...
```

핵심:
- Claude는 "지금 이 코드"만 보는 게 아님
- "왜 Passport를 선택했는지" 알고 있음
- "지금까지 뭘 했는지" 알고 있음
- 맥락 있는 답변

---

## 실전 운영 가이드

### 일일 워크플로우

#### 아침 (09:00)

**1. 프로젝트로 이동**
```bash
cd ~/projects/trading-system
```

**2. 오늘 계획 기록**
```bash
m "오늘 목표: GitHub OAuth 완료 및 테스트"
```

**3. 어제 뭐 했는지 확인 (선택)**
```bash
cat .memory/timeline/$(date -d yesterday +%Y-%m/%d).md
```

#### 작업 중 (09:00-17:00)

**계속 기록:**
```bash
m "GitHub OAuth Strategy 설정 시작"
m "CLIENT_ID, SECRET 환경 변수 추가"
m "callback URL 설정: /api/auth/github/callback"
```

**막힐 때 검색:**
```bash
ms "callback URL"
# → 과거 Google OAuth 때 어떻게 했는지 확인

ms --with-kb "OAuth scope"
# → 개인 KB에 정리된 OAuth 개념 확인
```

**Claude Code 활용:**
```bash
claude "GitHub OAuth scope 권장 설정은?"
# → Claude는 자동으로 컨텍스트 활용
```

**중요한 결정:**
```bash
m "결정: scope는 user:email, read:user만"
m "이유: 최소 권한 원칙, 지금은 이것만 필요"
```

**완료:**
```bash
m "GitHub OAuth 기본 구현 완료"
m "테스트: 로컬에서 성공"
```

**막힌 것:**
```bash
m "문제: 프로덕션 환경에서 callback 안 됨"
m "원인 조사 필요"
```

#### 저녁 (18:00)

**오늘 한 일 확인:**
```bash
cat .memory/timeline/$(date +%Y-%m/%d).md
```

**내일 계획:**
```bash
m "내일: callback URL 프로덕션 이슈 해결"
m "체크: HTTPS 설정, 도메인 설정 확인"
```

### 주간 워크플로우

#### 주말 정리 (1-2시간)

**Step 1: Timeline 리뷰**

```bash
# 이번 주 전체 보기
cat .memory/timeline/2025-11/{08..12}.md | less
```

읽으면서 메모:
- 중요한 결정들
- 배운 개념들
- 완료한 작업들
- 해결한 문제들

**Step 2: 결정 사항 승격**

중요한 결정을 찾아서 `decisions.md`로:

```bash
# 예: GitHub scope 결정
cat >> .memory/modules/auth/decisions.md << 'EOF'

## 2025-11-12: GitHub OAuth Scope
**결정:** `user:email`, `read:user`
**이유:**
- 최소 권한 원칙
- 현재 요구사항: 이메일과 기본 프로필만
- 나중에 필요하면 확장 가능

**컨텍스트:** [[time:2025-11/12#14:00]]
EOF
```

**Step 3: 현재 상태 업데이트**

```bash
cat >> .memory/modules/auth/current.md << 'EOF'

## 2025-11-12 주간
### Completed
- [x] GitHub OAuth 기본 구현
- [x] 로컬 테스트 완료

### Issues
- [ ] 프로덕션 callback 이슈 (다음주)

### Next
- [ ] Callback URL 프로덕션 수정
- [ ] 테스트 케이스 추가
- [ ] 에러 처리 강화
EOF
```

**Step 4: 개념 정리 (필요시)**

이번 주 새로 배운 개념이 있다면 개인 KB에:

```bash
# 예: OAuth scope 개념
cat > ~/memory/personal/concepts/oauth/scope.md << 'EOF'
---
type: concept
domain: oauth
timeline_refs:
  - [[time:2025-11/12#14:00]]
---

# OAuth Scope

## 정의
OAuth에서 앱이 요청하는 권한의 범위

## 원칙
**최소 권한 (Principle of Least Privilege)**
- 필요한 것만 요청
- 사용자 신뢰 증가
- 보안 위험 감소

## 실전 경험
GitHub OAuth 구현 시:
- `user:email`: 이메일 접근
- `read:user`: 기본 프로필 읽기
- 더 많은 권한은 나중에 추가 가능

## 학습 자료
- [[time:2025-11/12]] - 실제 적용
EOF
```

### 월간 워크플로우

#### 월말 회고 (1-2시간)

**Step 1: 통계 확인**

```bash
# 이번 달 기록 수
find .memory/timeline/2025-11 -name "*.md" -exec wc -l {} + | tail -1

# 모듈별 활동
ls -la .memory/modules/*/current.md
```

**Step 2: 주요 성과 정리**

```bash
cat > .memory/timeline/2025-11-review.md << 'EOF'
# 2025년 11월 회고

## 주요 성과
- OAuth 통합 완료 (Google, GitHub)
- 세션 기반 인증 구현
- Passport.js 도입

## 주요 결정
- [[modules/auth/decisions.md#2025-11-02]] 세션 vs JWT
- [[modules/auth/decisions.md#2025-11-12]] Passport.js 선택

## 배운 것
- OAuth 2.0 플로우
- Passport.js 전략 패턴
- 최소 권한 원칙

## 다음 달 목표
- OAuth 에러 처리 강화
- 비밀번호 재설정 구현
- 테스트 커버리지 90% 달성
EOF
```

**Step 3: 아카이브 (선택)**

오래된 프로젝트나 완료된 모듈:

```bash
# 옵션 1: 프로젝트 전체 아카이브
mv ~/projects/old-project ~/archive/projects/

# 옵션 2: 모듈 상태 변경
# modules/old-feature/module.md
---
status: archived  # active → archived
archived_date: 2025-11-30
archived_reason: 기능 제거됨
---
```

### 계절별 워크플로우

#### 분기 회고 (분기마다, 2-3시간)

프로젝트 전반 리뷰:

```bash
# Q4 2025 회고
cat > .memory/2025-Q4-review.md << 'EOF'
# 2025 Q4 회고

## 완료한 프로젝트
- Trading System v1.0 출시
- User Authentication 완성

## 주요 기술 결정
1. 세션 기반 인증 (vs JWT)
2. Passport.js 채택
3. Redis 세션 저장소

## 배운 개념들
- OAuth 2.0
- 전략 패턴 실전 적용
- 세션 관리 Best Practices

## 2025 Q1 목표
- 트레이딩 알고리즘 고도화
- 실시간 데이터 처리
- 모바일 앱 프로토타입
EOF
```

---

## 도구와 명령어

### 기본 CLI 설치

**설치 파일: `~/.bashrc` 또는 `~/.zshrc`**

```bash
# ============================================
# Memory System CLI
# ============================================

# 루트 찾기
_find_memory_root() {
  local dir="$PWD"
  while [[ "$dir" != "/" ]]; do
    [[ -d "$dir/.memory" ]] && echo "$dir/.memory" && return 0
    dir="$(dirname "$dir")"
  done
  return 1
}

# KB 루트 찾기
_get_kb_root() {
  local root="$(_find_memory_root)"
  if [[ -n "$root" && -f "$root/../kb.lock" ]]; then
    kb=$(awk -F': ' '/^kb_root:/ {print $2}' "$root/../kb.lock")
    [[ -n "$kb" ]] && echo "$kb" && return 0
  fi
  echo "$HOME/memory/personal"
}

# 기록 (MVP 버전)
m() {
  local root="$(_find_memory_root)"
  if [[ -z "$root" ]]; then
    echo "❌ Not in a project with .memory/"
    echo "Run: minit"
    return 1
  fi
  
  local d=$(date +%Y-%m/%d)
  local t=$(date +%H:%M)
  local file="$root/timeline/$d.md"
  
  mkdir -p "$(dirname "$file")"
  echo "- $t | $*" >> "$file"
  echo "✓ Recorded to timeline/$d.md"
}

# 검색
ms() {
  local root="$(_find_memory_root)"
  if [[ -z "$root" ]]; then
    echo "❌ Not in a project"
    return 1
  fi
  
  case "$1" in
    --with-kb)
      shift
      local kb="$(_get_kb_root)"
      echo "🔍 Searching: local + KB"
      rg --heading --line-number "$@" "$root" "$kb"
      ;;
    --all)
      shift
      local kb="$(_get_kb_root)"
      echo "🔍 Searching: all projects + KB"
      rg --heading --line-number "$@" ~/projects/*/.memory "$kb" 2>/dev/null
      ;;
    *)
      echo "🔍 Searching: local only"
      rg --heading --line-number "$@" "$root"
      ;;
  esac
}

# 초기화
minit() {
  if [[ -d .memory ]]; then
    echo "⚠️  .memory already exists"
    return 1
  fi
  
  mkdir -p .memory/{timeline,modules,concepts,.index}
  
  cat > .memory/kb.lock << 'EOF'
kb_root: ~/memory/personal
mode: float
EOF
  
  echo ".memory/.index/" >> .gitignore
  
  echo "✓ Initialized .memory/"
  echo "  - timeline/  (daily logs)"
  echo "  - modules/   (module contexts)"
  echo "  - concepts/  (project concepts)"
  echo ""
  echo "Try: m 'first entry'"
}

# 상태 확인
mstatus() {
  local root="$(_find_memory_root)"
  if [[ -z "$root" ]]; then
    echo "❌ Not in a project"
    return 1
  fi
  
  echo "📊 Memory Status"
  echo "━━━━━━━━━━━━━━━━━━━━"
  echo ""
  echo "📝 Timeline:"
  echo "  Files: $(find "$root/timeline" -name "*.md" -type f 2>/dev/null | wc -l)"
  echo "  Lines: $(find "$root/timeline" -name "*.md" -exec wc -l {} + 2>/dev/null | tail -1 | awk '{print $1}')"
  echo ""
  echo "🧩 Modules:"
  echo "  Count: $(find "$root/modules" -maxdepth 1 -mindepth 1 -type d 2>/dev/null | wc -l)"
  echo ""
  echo "💡 Concepts:"
  echo "  Count: $(find "$root/concepts" -name "*.md" -type f 2>/dev/null | wc -l)"
  echo ""
  echo "🕐 Recent (7 days):"
  echo "  Active: $(find "$root/timeline" -name "*.md" -mtime -7 -type f 2>/dev/null | wc -l) days"
}

# 오늘 기록 보기
mtoday() {
  local root="$(_find_memory_root)"
  if [[ -z "$root" ]]; then
    echo "❌ Not in a project"
    return 1
  fi
  
  local today=$(date +%Y-%m/%d)
  local file="$root/timeline/$today.md"
  
  if [[ -f "$file" ]]; then
    cat "$file"
  else
    echo "No entries for today yet."
    echo "Start recording: m 'your first entry'"
  fi
}

# 이번 주 보기
mweek() {
  local root="$(_find_memory_root)"
  if [[ -z "$root" ]]; then
    echo "❌ Not in a project"
    return 1
  fi
  
  echo "📅 This Week's Timeline"
  echo "━━━━━━━━━━━━━━━━━━━━━━"
  find "$root/timeline" -name "*.md" -mtime -7 -type f | sort | while read file; do
    echo ""
    echo "## $(basename $file .md)"
    cat "$file"
  done
}
```

**적용**

```bash
# 추가 후
source ~/.bashrc

# 또는
source ~/.zshrc
```

### 명령어 레퍼런스

#### m - 기록

```bash
# 기본 사용
m "OAuth 구현 시작"

# 긴 내용
m "결정: Passport.js 선택
이유: 전략 패턴, 확장성, 커뮤니티"

# 여러 줄 (heredoc)
m << 'EOF'
OAuth 구현 완료
- Google: 완료
- GitHub: 완료
- 테스트: 통과
EOF
```

#### ms - 검색

```bash
# 로컬만 (기본)
ms "OAuth"
ms "keyword"

# 정규식
ms "결정:.*Passport"

# KB 포함
ms --with-kb "strategy pattern"

# 모든 프로젝트
ms --all "Redis"

# 타입 필터 (구현 예정)
ms --type concept "pattern"
```

#### minit - 초기화

```bash
cd ~/projects/new-project
minit
# → .memory/ 구조 생성
# → kb.lock 생성
# → .gitignore 업데이트
```

#### mstatus - 상태 확인

```bash
mstatus
# 출력:
# 📊 Memory Status
# ━━━━━━━━━━━━━━━━━━━━
# 📝 Timeline:
#   Files: 45
#   Lines: 1,234
# 🧩 Modules:
#   Count: 5
# ...
```

#### mtoday - 오늘 보기

```bash
mtoday
# 오늘의 Timeline 출력
```

#### mweek - 이번 주 보기

```bash
mweek
# 최근 7일 Timeline 출력
```

### Claude Code 통합

#### 컨텍스트 빌더 설치

```bash
# .claude/build-context.sh
cat > .claude/build-context.sh << 'SCRIPT'
#!/bin/bash

OUTPUT=".claude/memory-context.md"
MEMORY_ROOT=".memory"

# 헤더
cat > "$OUTPUT" << 'HEADER'
# Project Memory Context

이 문서는 .memory/에서 자동 생성되었습니다.

---

HEADER

# 최근 7일 Timeline
echo "## Recent Timeline (Last 7 Days)" >> "$OUTPUT"
echo "" >> "$OUTPUT"

find "$MEMORY_ROOT/timeline" -name "*.md" -type f -mtime -7 2>/dev/null | sort | while read file; do
  date=$(basename "$file" .md)
  echo "### $date" >> "$OUTPUT"
  cat "$file" >> "$OUTPUT"
  echo "" >> "$OUTPUT"
done

# 모든 모듈
echo "## Modules" >> "$OUTPUT"
echo "" >> "$OUTPUT"

if [[ -d "$MEMORY_ROOT/modules" ]]; then
  find "$MEMORY_ROOT/modules" -mindepth 1 -maxdepth 1 -type d 2>/dev/null | sort | while read module_dir; do
    module_name=$(basename "$module_dir")
    echo "### Module: $module_name" >> "$OUTPUT"
    echo "" >> "$OUTPUT"
    
    for doc in module.md current.md decisions.md dependencies.md interface.md; do
      if [[ -f "$module_dir/$doc" ]]; then
        echo "#### ${doc%.md}" >> "$OUTPUT"
        cat "$module_dir/$doc" >> "$OUTPUT"
        echo "" >> "$OUTPUT"
      fi
    done
    
    echo "---" >> "$OUTPUT"
    echo "" >> "$OUTPUT"
  done
fi

# 개념
echo "## Concepts" >> "$OUTPUT"
echo "" >> "$OUTPUT"

if [[ -d "$MEMORY_ROOT/concepts" ]]; then
  find "$MEMORY_ROOT/concepts" -name "*.md" -type f 2>/dev/null | sort | while read file; do
    concept_name=$(basename "$file" .md)
    echo "### Concept: $concept_name" >> "$OUTPUT"
    cat "$file" >> "$OUTPUT"
    echo "" >> "$OUTPUT"
  done
fi

echo "✓ Context built: $OUTPUT"
SCRIPT

chmod +x .claude/build-context.sh
```

#### 래퍼 함수 (자동 컨텍스트 갱신)

```bash
# ~/.bashrc에 추가

claude() {
  if [[ -f ".claude/build-context.sh" ]]; then
    echo "🔄 Refreshing memory context..."
    ./.claude/build-context.sh
    echo ""
  fi
  
  claude-code "$@"
}
```

#### 사용

```bash
# 자동으로 컨텍스트 갱신 후 실행
claude "OAuth 구현 개선 방법"

# Claude는 자동으로 알고 있음:
# - 최근 작업 (Timeline)
# - 모듈 상태 (current.md)
# - 과거 결정 (decisions.md)
```

---

## 실전 예시

### 예시 1: 새 프로젝트 시작

#### Day 0: 프로젝트 생성

```bash
# 프로젝트 생성
mkdir ~/projects/ecommerce-api
cd ~/projects/ecommerce-api

# Git 초기화
git init

# Memory 초기화
minit

# 첫 기록
m "프로젝트 생성: E-commerce API"
m "목표: REST API 서버, 상품/주문/결제"
```

#### Day 1: 기술 스택 결정

```bash
m "기술 스택 조사 시작"
m "검토: Express vs Fastify vs NestJS"

# 오전 작업
m "Express: 간단, 생태계 큼, 하지만 구조 느슨"
m "Fastify: 빠름, 타입 안전, 하지만 생태계 작음"
m "NestJS: 구조 강제, DI, 하지만 학습 곡선"

# 점심 후 결정
m "결정: NestJS 선택"
m "이유: 프로젝트 크기 예상됨, 팀 협업, 타입 안전"
```

#### 주말: 정리

```bash
# 모듈 생성
mkdir -p .memory/modules/api-server

# 모듈 정의
cat > .memory/modules/api-server/module.md << 'EOF'
---
type: module
created: 2025-11-15
---

# API Server Module

## Purpose
E-commerce REST API 서버

## Tech Stack
- NestJS
- PostgreSQL
- Redis (캐싱)
- JWT (인증)

## Scope
- 상품 관리 (CRUD)
- 주문 처리
- 결제 통합
- 사용자 인증
EOF

# 결정 기록
cat > .memory/modules/api-server/decisions.md << 'EOF'
# Key Decisions

## 2025-11-15: 프레임워크 선택
**결정:** NestJS
**대안:**
- Express: 간단하지만 구조 부족
- Fastify: 빠르지만 생태계 작음

**이유:**
1. 프로젝트 규모 예상 (중-대규모)
2. 팀 협업 용이 (강제된 구조)
3. 타입 안전성 (TypeScript 기반)
4. DI, 데코레이터로 깔끔한 코드

**트레이드오프:**
- 학습 곡선: 수용 (문서 좋음)
- 초기 복잡도: 수용 (장기적 이득)

**컨텍스트:** [[time:2025-11/15#14:00]]
EOF
```

### 예시 2: 기존 프로젝트에 도입

**상황: 3개월 진행 중인 프로젝트**

```bash
cd ~/projects/existing-project

# Memory 초기화
minit

# 기존 README를 모듈로
cat > .memory/modules/main/module.md << 'EOF'
---
type: module
created: 2025-08-01  # 프로젝트 시작일
---

# Main Application

## Purpose
(README 내용 복사)

## Current Status
- Version: v0.5.0
- Users: ~100
- 주요 기능 완성, 최적화 단계
EOF

# 앞으로는 Timeline 사용
m "Memory 시스템 도입"
m "앞으로 모든 작업 기록"
```

#### 점진적 마이그레이션

**Week 1: Timeline만**
```bash
# 매일 기록만
m "버그 수정: 로그인 이슈"
m "성능 개선: 쿼리 최적화"
```

**Week 2: 모듈 추가**
```bash
# 주요 모듈 문서화
mkdir -p .memory/modules/authentication
mkdir -p .memory/modules/database

# 간단하게 시작
echo "# Authentication Module" > .memory/modules/authentication/module.md
```

**Week 3-4: 결정 사항 역추적**

Git log를 보면서 중요한 결정들을 `decisions.md`로:

```bash
git log --grep="결정" --all --oneline

# 각 커밋의 메시지를 decisions.md로
cat > .memory/modules/authentication/decisions.md << 'EOF'
# Key Decisions

## 2025-09-10: JWT 도입
(커밋 메시지 참조)
**결정:** JWT 사용
**이유:** ...
EOF
```

### 예시 3: 개념 학습

**시나리오: Redis 학습**

**Day 1: 첫 접촉**
```bash
m "Redis 조사 시작"
m "인메모리 데이터 구조 저장소"
m "왜 빠른가: 메모리, C로 작성"
```

**Day 2: 데이터 타입**
```bash
m "Redis 5가지 기본 타입"
m "1. String: 가장 기본"
m "2. List: 순서 있는 문자열"
m "3. Set: 중복 없는 집합"
m "4. Hash: 필드-값 맵"
m "5. Sorted Set: 점수 기반 정렬"
```

**Day 3: 실전 적용**
```bash
m "프로젝트에 Redis 도입 검토"
m "사용 케이스: 세션 저장, 캐싱"
```

**주말: 개념 정리**

```bash
# 개인 KB에 정리
cat > ~/memory/personal/concepts/databases/redis.md << 'EOF'
---
type: concept
domain: databases
tags: [cache, nosql, inmemory]
timeline_refs:
  - [[time:2025-11/15]]
  - [[time:2025-11/16]]
  - [[time:2025-11/17]]
---

# Redis

## 정의
인메모리 데이터 구조 저장소

## 학습 과정

### 2025-11-15: 첫 인상
빠른 키-값 저장소 정도로 이해

### 2025-11-16: 데이터 타입
5가지 기본 타입:
- String, List, Set, Hash, Sorted Set

각각 특화된 명령어 세트

### 2025-11-17: 실전 적용
세션 저장과 캐싱에 적합:
- 메모리라 빠름
- TTL 지원
- 단순한 API

## 핵심 통찰
"단순한 캐시가 아니라 
 다양한 데이터 구조를 제공하는 
 범용 인메모리 저장소"

## 실전 적용
- [[projects/ecommerce-api]] - 세션 저장
- [[projects/analytics]] - 실시간 집계
EOF
```

**결과:**

이제 다른 프로젝트에서:
```bash
ms --with-kb "Redis session"
# → 과거 학습 내용 발견
# → 빠르게 재활용
```

---

## 안전과 확장

### Phase 분리

#### Phase 1: MVP (Day 0-14)

**목표:** 습관 형성

**기능:**
- 기본 `m`, `ms` 명령어
- 단순 append (atomic 아님)
- 수동 정리

**측정:**
- 일일 캡처 ≥ 5
- 주간 검색 ≥ 20

#### Phase 2: 하드닝 (Day 15-30)

**조건:** Phase 1 목표 달성

**추가 기능:**

**P0 (필수):**

1. **Atomic Write + flock**

```bash
m() {
  local root="$(_find_memory_root)"
  local file="$root/timeline/$(date +%Y-%m/%d).md"
  local tmp="$file.tmp.$$"
  
  # 락 획득
  exec 9>>"$file.lock"
  if flock -n 9; then
    echo "- $(date +%H:%M) | $*" >> "$tmp"
    mv "$tmp" "$file"
    flock -u 9
  else
    # 실패 시 격리
    mkdir -p "$root/timeline/_quarantine"
    echo "- $(date +%H:%M) | $*" >> "$root/timeline/_quarantine/$(date +%s).md"
    echo "⚠️  Quarantined (lock failed)"
  fi
}
```

2. **Monotonic ID**

```bash
generate_id() {
  echo "$(date -u +%Y%m%dT%H%M%S.%3N)-$$-$(openssl rand -hex 2)"
}
```

3. **Privacy 필터**

```python
def filter_privacy(documents):
    return [doc for doc in documents if doc.privacy != "secret"]
```

**P1 (권장):**

1. **감사 로그**

```bash
# .memory/.index/audit.log
2025-11-15T14:30:00Z | AUTO | LINK_SUGGEST | kb:market-efficiency → modules/trading
2025-11-15T14:31:00Z | USER | LINK_ACCEPT | kb:market-efficiency → modules/trading
```

2. **한국어 검색 (옵션)**

```python
# 형태소 분석 + 동의어 사전
from konlpy.tag import Mecab

def normalize_korean(text):
    mecab = Mecab()
    return ' '.join(mecab.morphs(text))
```

#### Phase 3: 지능화 (Day 31+)

**조건:** Phase 2 안정

**추가 기능:**

1. **자동 요약**

```python
async def after_claude_conversation():
    summary = await claude.summarize_conversation()
    if summary.is_important():
        m(f"AI 대화: {summary.title}")
```

2. **스마트 승격 제안**

```python
def suggest_promotion(timeline_entry):
    if is_decision(timeline_entry):
        return ("decisions.md", confidence=0.85)
    elif is_concept(timeline_entry):
        return ("concepts/", confidence=0.75)
```

3. **의미 검색**

```python
# 임베딩 기반 Top-K
def semantic_search(query, top_k=5):
    query_emb = embed(query)
    scores = cosine_similarity(query_emb, all_embeddings)
    return top_k_documents(scores)
```

### 실패 모드와 복구

#### 캡처 유실

**증상:**
- Timeline 파일 손상
- 기록 일부 누락

**원인:**
- 동시 쓰기
- 전원 차단
- 디스크 가득 참

**복구:**
```bash
# Quarantine 확인
ls .memory/timeline/_quarantine/

# 복구 가능한 것 병합
cat .memory/timeline/_quarantine/*.md >> .memory/timeline/$(date +%Y-%m/%d).md
```

**예방:** Phase 2 atomic write

#### 검색 실패

**증상:**
- "분명 기록했는데 안 나옴"

**원인:**
- 한국어 형태 변형 ("탄력성" vs "탄력적")
- 오타
- 다른 프로젝트에 기록

**해결:**
```bash
# 넓게 검색
ms --all "탄력"

# 날짜로 찾기
cat .memory/timeline/2025-11/15.md | grep -i "탄력"
```

**예방:** Phase 2 형태소 인덱스

#### 그래프 오염

**증상:**
- 무관한 자동 링크
- 잘못된 분류

**원인:**
- 자동화 너무 이른 시점 도입
- 신뢰도 임계값 너무 낮음

**복구:**
```bash
# 감사 로그 확인
grep "AUTO" .memory/.index/audit.log

# 되돌리기
# (수동으로 링크 제거)
```

**예방:**
- Phase 1에서 자동화 금지
- Phase 2+ 에서 신중히 도입

#### Context 과다

**증상:**
- Claude Code 답변 품질 저하
- "관련 없는 얘기를 해"

**원인:**
- 너무 많은 문서 로드
- 관련성 낮은 정보 포함

**해결:**
```bash
# build-context.sh 수정
# Timeline 7일 → 3일
find "$MEMORY_ROOT/timeline" -mtime -3

# 또는 스마트 선택
./claude/build-context-smart.sh "OAuth"
```

**예방:** Top-K 제한, 의미 검색

---

## 시작하기

### 15분 퀵스타트

**Step 1: 개인 KB 생성 (1분)**

```bash
mkdir -p ~/memory/personal/{concepts,resources,.index}
```

**Step 2: CLI 설치 (5분)**

```bash
# ~/.bashrc 또는 ~/.zshrc에 붙여넣기
# (위의 "도구와 명령어" 섹션 참조)

# 적용
source ~/.bashrc
```

**Step 3: 프로젝트 초기화 (2분)**

```bash
# 기존 프로젝트 또는 새 프로젝트
cd ~/projects/my-project

# 초기화
minit
```

**Step 4: 첫 기록 (3분)**

```bash
# 테스트 기록
m "Memory 시스템 시작"
m "첫 테스트 기록"
m "오늘 목표: 시스템 익히기"

# 확인
mtoday
```

**Step 5: 검색 테스트 (2분)**

```bash
# 방금 기록한 것 찾기
ms "시스템"

# 결과 확인
```

**Step 6: 상태 확인 (2분)**

```bash
# 통계
mstatus

# 프로젝트 두 개 더 만들기
cd ~/projects/project-b
minit
m "프로젝트 B 시작"

cd ~/projects/project-c
minit
m "프로젝트 C 시작"
```

### 첫 주 계획

#### Day 1-3: 기록만

**목표:** 습관 형성

```bash
# 매일 최소 5개
m "오늘 한 일 1"
m "오늘 한 일 2"
m "오늘 한 일 3"
m "오늘 한 일 4"
m "오늘 한 일 5"
```

**체크:**
```bash
mstatus
# → Timeline Files: 3 (OK)
```

#### Day 4-5: 검색 연습

**목표:** 검색 습관

```bash
# 과거 기록 찾기
ms "keyword"

# 자주 검색하기
# (주간 목표: 20회+)
```

#### Day 6-7: 첫 정리

**목표:** 승격 경험

```bash
# 이번 주 Timeline 보기
mweek

# 중요한 것 하나 골라 모듈로
mkdir -p .memory/modules/my-module
cat > .memory/modules/my-module/decisions.md << 'EOF'
# Key Decisions

## 2025-11-XX: 첫 결정
**결정:** ...
**이유:** ...
**컨텍스트:** [[time:2025-11/XX#HH:MM]]
EOF
```

### 첫 달 목표

**Week 1-2: 기본 습관**
- ☑ 일일 캡처 ≥ 5 (10/14일 달성)
- ☑ 주간 검색 ≥ 20
- ☑ Timeline 익숙해지기

**Week 3: 구조화 시작**
- ☑ 모듈 1-2개 생성
- ☑ decisions.md 작성 경험
- ☑ 개인 KB에 개념 1개 추가

**Week 4: 통합**
- ☑ Claude Code 연동
- ☑ KB 참조 경험
- ☑ 프로젝트 2-3개로 확장

### 체크리스트

**필수 설정**
- ☐ ~/memory/personal/ 생성
- ☐ CLI 설치 (m, ms, minit 등)
- ☐ 프로젝트 1개 minit
- ☐ 첫 기록 3개
- ☐ 검색 테스트

**첫 주**
- ☐ 매일 기록 (목표: 5+/일)
- ☐ 검색 사용 (목표: 20+/주)
- ☐ mtoday, mweek 명령어 익히기

**첫 달**
- ☐ 모듈 1개 생성
- ☐ decisions.md 작성
- ☐ 개인 KB 개념 1개
- ☐ Claude Code 연동
- ☐ 프로젝트 2-3개로 확장

---

## 부록

### FAQ

**Q: 모든 생각을 다 기록해야 하나요?**

A: 아니요. "기록할 가치가 있다"고 느껴지는 것만 기록하세요.

기록 대상:
- 중요한 결정
- 새로 배운 것
- 막힌 문제와 해결
- 나중에 참조할 것

기록 안 해도 됨:
- 일상적인 루틴 작업
- 자명한 것
- 휘발성 높은 것

**Q: Timeline이 너무 길어지면?**

A: 괜찮습니다. Timeline은 길어도 됩니다.

- 검색으로 찾습니다 (`ms "keyword"`)
- 중요한 것만 Modules/Concepts로 승격
- 오래된 것은 자동으로 검색에서 제외됨 (mtime 기반)

**Q: 프로젝트 삭제하면?**

A: `.memory/`도 함께 삭제됩니다.

보존하려면:
```bash
# 아카이브
mv ~/projects/old-project ~/archive/projects/old-project
```

**Q: 개인 KB에 무엇을 넣나요?**

A: 프로젝트와 무관하게 재사용 가능한 지식:
- 개념, 이론, 패턴
- 학습 노트
- 참고 자료 요약

프로젝트 특화된 것은 프로젝트 `.memory/`에

**Q: 언제 정리하나요?**

A:
- 필수: 주말 1-2시간 (주간 리뷰)
- 선택: 월말, 분기말 (회고)

"정리 안 하면 안 되나요?" → 당장은 괜찮지만, 장기적으로 Timeline만 쌓이면 활용도 떨어짐

**Q: Claude Code 없이도 가능한가요?**

A: 네. 핵심은 기록과 정리입니다.

Claude Code는:
- 있으면: LLM이 맥락 이해 (편리)
- 없어도: 검색으로 충분히 활용 가능

### 용어집

**Timeline**
- 시간순으로 모든 작업을 기록하는 1차 생산층
- 일별 마크다운 파일

**Module**
- 코드 모듈, 시스템 컴포넌트 등의 구조화된 단위
- 5개 파일 (module, current, decisions, dependencies, interface)

**Concept**
- 추상적 지식, 이론, 패턴
- 프로젝트 특화 또는 범용

**KB (Knowledge Base)**
- 개인 지식 저장소 (`~/memory/personal/`)
- 여러 프로젝트에서 참조 가능

**승격 (Promotion)**
- Timeline 항목을 Modules/Concepts로 이동
- 1차 기록 → 2차 정리

**스코프 (Scope)**
- 검색 범위: local, --with-kb, --all

**링크 (Link)**
- 문서 간 연결
- ID 기반 (`[[kb:id]]`, `[[doc:id]]`)

### 참고 자료

**영감을 준 시스템들**
- Git: 버전 관리의 시간-공간 통합
- Zettelkasten: 원자적 노트와 링크
- PARA: 프로젝트 중심 조직
- GTD: 포착의 중요성
- Obsidian: 링크 기반 지식 그래프

**철학적 배경**
- Luhmann의 Zettelkasten: 외부 두뇌로서의 노트
- Building a Second Brain: 디지털 지식 관리
- How to Take Smart Notes: 효과적인 노트 작성

---

## 마지막 한 줄:

**시간으로 과정을 보존하고, 공간으로 현재를 구조화하며, 링크로 둘을 연결한다. 프로젝트는 격리하되 지식은 공유하고, 로컬을 우선하되 필요시 확장한다. 0.5초로 포착하고, 주말에 정리하며, 평생 활용한다.**

**지금 시작하세요.**
